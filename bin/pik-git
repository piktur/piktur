#!/usr/bin/env ruby
# frozen_string_literal: true

$LOAD_PATH.unshift File.expand_path('../piktur/lib', __dir__)

require_relative Fie.expand_path('git', __dir__)

Signal.trap('INT') { puts; exit(1) }

Git.call
